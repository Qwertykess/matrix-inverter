package main;

public class WindowEvents {
    // Method for matrix size input >= 2 && <= 8 ------ -> matrixSize will nested for loop textfield until n*n. each box is saved to matrixBoxValue[i][j] 

    // Method for addlistener for inversion method buttons. 

    // Method for addllistener for calculate. Call gaussian elimination if RREF, not sure about adjoint.
    
    // Method for addlistener for clear, clearing all inputs to default
}
